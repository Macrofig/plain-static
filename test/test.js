
var assert = require('assert');

describe('plain-static', function () {
  describe('plain-static/clean', function () {
    describe('when clean is called', function () {
      it('cleans a folder', function () {
        assert.equal(1, 1);
      });
    });
  });
});
